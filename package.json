{
    "name": "nulltrace",
    "version": "1.0.0",
    "private": true,
    "description": "Zero-Knowledge Ephemeral Secret Sharing Service",
    "scripts": {
        "dev": "wrangler dev src/api/index.ts",
        "deploy": "wrangler deploy src/api/index.ts",
        "test": "vitest run",
        "test:coverage": "vitest run --coverage",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {},
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240208.0",
        "typescript": "^5.3.3",
        "vitest": "^1.3.1",
        "wrangler": "^3.30.0"
    }
}